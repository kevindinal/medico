// Get the submenu element by its ID
let subMenu = document.getElementById("subMenu");

function toggleMenu(){ // Function to toggle the open-menu class on the submenu
    subMenu.classList.toggle("open-menu");
}
